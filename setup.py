from setuptools import setup

setup(name='sensor_simulator',
      version='0.1',
      description='Python simulation of a gateway with multiple sensor nodes to test REST api of server.',
      author='Vincent Ceulemans, Matthias Mombaerts',
      packages=['sensor_simulator'],
      zip_safe=False)
